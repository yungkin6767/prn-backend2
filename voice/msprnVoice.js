// Voice configuration for Ms. PRN
export const msprnVoice = {
  voiceConfig: {
    voice: {
      name: "Piper", // feminine, smooth
      languageCode: "en-US",
    }
  }
};

// Export voiceConfig for backward compatibility
export const voiceConfig = msprnVoice.voiceConfig;

