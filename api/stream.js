import express from "express";

const router = express.Router();

// Streaming endpoint placeholder
router.get("/", (req, res) => {
  res.json({ message: "Stream API endpoint - Coming soon" });
});

export default router;

